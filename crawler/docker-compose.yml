version: "3.8"
services:
  server:
    build:
      dockerfile: crawler/Dockerfile
      context: ../
    volumes:
      - ./output:/usr/src/app/output
    ports:
      - "3000:3000"
    env_file:
      - .env
